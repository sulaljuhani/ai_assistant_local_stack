# Backend API URL
# For local development with backend running in Docker:
VITE_BACKEND_URL=http://localhost:8000

# For Docker deployment (internal network):
# VITE_BACKEND_URL=http://langgraph-agents:8000

# For production deployment (replace with your domain):
# VITE_BACKEND_URL=https://your-domain.com/api

# Note: User ID and workspace are hardcoded in code (single-user system)
# USER_ID: 00000000-0000-0000-0000-000000000001
# WORKSPACE: default
